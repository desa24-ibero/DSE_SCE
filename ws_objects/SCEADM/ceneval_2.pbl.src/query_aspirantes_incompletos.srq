$PBExportHeader$query_aspirantes_incompletos.srq
PBSELECT( VERSION(400) TABLE(NAME="aspiran_backup" )  TABLE(NAME="general_backup" )  TABLE(NAME="padres_backup" )  TABLE(NAME="status" )  TABLE(NAME="estatus_sol_reg" ) COLUMN(NAME="aspiran_backup.folio") COLUMN(NAME="aspiran_backup.clv_ver") COLUMN(NAME="aspiran_backup.clv_per") COLUMN(NAME="aspiran_backup.anio") COLUMN(NAME="aspiran_backup.status") COLUMN(NAME="estatus_sol_reg.status_reg") COLUMN(NAME="estatus_sol_reg.descripcion") COLUMN(NAME="general_backup.apaterno") COLUMN(NAME="general_backup.amaterno") COLUMN(NAME="general_backup.nombre") COLUMN(NAME="general_backup.sexo") COLUMN(NAME="general_backup.telefono") COLUMN(NAME="general_backup.calle") COLUMN(NAME="general_backup.codigo_pos") COLUMN(NAME="general_backup.colonia") COLUMN(NAME="general_backup.email") COLUMN(NAME="padres_backup.apaterno") COLUMN(NAME="padres_backup.amaterno") COLUMN(NAME="padres_backup.nombre") COLUMN(NAME="padres_backup.sexo") COLUMN(NAME="padres_backup.telefono") COLUMN(NAME="padres_backup.calle") COLUMN(NAME="padres_backup.codigo_pos") COLUMN(NAME="padres_backup.colonia")    JOIN (LEFT="aspiran_backup.folio"    OP ="="RIGHT="general_backup.folio" )    JOIN (LEFT="general_backup.folio"    OP ="="RIGHT="padres_backup.folio" )    JOIN (LEFT="aspiran_backup.clv_ver"    OP ="="RIGHT="general_backup.clv_ver" )    JOIN (LEFT="aspiran_backup.clv_per"    OP ="="RIGHT="general_backup.clv_per" )    JOIN (LEFT="aspiran_backup.anio"    OP ="="RIGHT="general_backup.anio" )    JOIN (LEFT="general_backup.clv_ver"    OP ="="RIGHT="padres_backup.clv_ver" )    JOIN (LEFT="general_backup.clv_per"    OP ="="RIGHT="padres_backup.clv_per" )    JOIN (LEFT="general_backup.anio"    OP ="="RIGHT="padres_backup.anio" )    JOIN (LEFT="aspiran_backup.status"    OP ="="RIGHT="status.status" )    JOIN (LEFT="aspiran_backup.status_reg"    OP ="="RIGHT="estatus_sol_reg.status_reg" )WHERE(    EXP1 ="aspiran_backup.clv_ver"   OP ="="    EXP2 =":an_clv_ver"    LOGIC ="and" ) WHERE(    EXP1 ="aspiran_backup.clv_per"   OP ="="    EXP2 =":an_clv_per"    LOGIC ="and" ) WHERE(    EXP1 ="aspiran_backup.anio"   OP ="="    EXP2 =":an_anio" ) ) ARG(NAME = "an_clv_ver" TYPE = number)  ARG(NAME = "an_clv_per" TYPE = number)  ARG(NAME = "an_anio" TYPE = number) 